# 换行符策略：Windows 上编辑、Linux 上部署，不显式声明就一定会踩坑。
#
# 起因：`.sh` 如果在 Windows 上被转成 CRLF，Linux 上执行会报
#   bash: ./deploy.sh: /bin/bash^M: bad interpreter: No such file or directory
# 反过来 `.bat` 如果在 Linux 上被转成 LF，Windows cmd 的某些分支又会出错。
# 所以按文件类型显式约定，让两个平台各拿自己需要的格式。

# 默认：交给 Git 自己判断文本文件，检出时用平台默认换行
* text=auto

# shell 脚本必须是 LF —— 否则 Linux 上的 shebang 会带着 \r 一起解析
*.sh text eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 容器相关文件必须是 LF。这两条不是洁癖，是会真出问题的：
# - `.dockerignore` 的行尾如果是 \r，排除规则会匹配不上（模式变成 ".env\r"），
#   于是 `.env` 照旧被 COPY 进镜像 —— 而且**不报错**，只是排除静默失效。
# - `Dockerfile` 里 `RUN` 的续行（`\` 结尾）遇上 CRLF 会断开，命令被截在半路。
# 这两类坑都只在「Windows 上检出、然后在别处构建」时才出现，本地很难复现。
Dockerfile text eol=lf
.dockerignore text eol=lf
*.yml text eol=lf

# 这些是二进制，别做任何换行转换
*.pt binary
*.png binary
*.jpg binary
*.ico binary
