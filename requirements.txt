# 运行依赖（聊天模块 + 后端 API 层）
# 刻意不包含 torch / numpy —— 只有情感分析模块才需要，见 requirements-ml.txt
streamlit>=1.30.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# 后端 API 层
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
redis>=5.0.0
# FastAPI 的 TestClient（starlette.testclient）依赖 httpx2，
# 没有它 CI 里 test_api.py 的 from fastapi.testclient import TestClient 会
# 直接 ImportError（本地旧版 fastapi 用 httpx 所以没暴露，CI 装最新版才暴露）。
httpx2>=0.1.0
